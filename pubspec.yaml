name: _workspace
publish_to: none

environment:
  sdk: ^3.12.0
  flutter: '>=3.44.0'

workspace:
  - packages/rrule_picker
  - packages/rrule_picker_example

dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_svg: ^2.3.0
  intl: any
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  kiri_check: ^1.3.1
  mocktail: ^1.0.5
  spot: ^0.18.0
