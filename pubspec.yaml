name: _workspace
publish_to: none

environment:
  sdk: ^3.13.0

workspace:
  - packages/rrule_picker
  - packages/rrule_picker/example

dependencies:
  cupertino_icons: ^1.0.9
  flutter:
    sdk: flutter
  intl: '>=0.20.3 <1.0.0'

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  kiri_check: ^1.3.1
  material_ui: ^1.0.0
  mocktail: ^1.0.5
  spot: ^0.20.0
