name: _workspace
publish_to: none

environment:
  sdk: ^3.12.0
  flutter: '>=3.44.0'

workspace:
  - packages/rrule_picker
  - packages/rrule_picker_example

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  kiri_check: ^1.3.1
